# 매불쇼 신입 작가 생존기 — 인수인계 문서

> 새 세션은 이 파일을 **가장 먼저** 읽을 것.
> 최종 갱신: 2026-07-28

---

## 1. 무엇인가

원본 [BLACKSITE](https://github.com/socialkim/blacksite-fps) 엔진을 포크해 만든
**비전투 내러티브 게임**. 최욱의 매불쇼를 소재로 한 팬메이드 패러디다.

- 플레이 — https://socialkim.github.io/maebulshow-game/
- 저장소 — https://github.com/socialkim/maebulshow-game
- 로컬 폴더 — `C:\Users\kimdu\claude\maebulshow-game`

**원본과 완전히 분리되어 있다.** 이쪽을 고쳐도 `fps-blacksite`에 영향이 없고, 반대도 마찬가지다.

---

## 2. 반드시 지켜야 할 원칙

이 프로젝트에는 **의도적으로 지킨 선**이 있다. 이후 작업에서도 유지할 것.

1. **실존 인물을 공격 대상으로 삼지 않는다.** 원본은 1인칭 슈터였지만 여기서는 전투를 전부 제거했다.
   적 캐릭터 0명, 무기 숨김, 사격 입력 차단 — 세 겹으로 막아뒀다.
2. **시사·정치 소재를 넣지 않는다.** 매불쇼는 시사 코너가 중심인 방송이지만,
   게임 소재는 '생방송을 준비하는 스튜디오 현장'으로만 한정했다.
3. **모든 대사는 창작임을 명시한다.** 시작 화면과 README 양쪽에 고지가 들어가 있다.
   실제 인물의 발언으로 오해될 여지가 있는 문장은 쓰지 않는다.
4. **상업적 사용 안 함.** 권리자 요청이 있으면 즉시 내린다는 문구를 README에 넣어뒀다.

---

## 3. 원본에서 바꾼 것 — 딱 세 파일

렌더링·조명·레벨·이동은 원본 그대로다. 즉 **그래픽 품질은 원본과 동일**하다.

```
src/core/Config.js     ai.count : 14 → 0        적 스폰 차단
src/main.js            Story 모듈을 마지막 단계로 추가
src/story/Story.js     신규 — 내러티브 레이어 전체 (이 파일 하나에 다 들어 있다)
```

### Story.js 가 하는 일

| 기능 | 구현 |
|---|---|
| 무기 숨김 | `weapon.viewmodelScene.visible = false` (three는 visible=false 루트를 통째로 건너뛴다) |
| 원본 HUD 숨김 | `#hud` 를 `display:none` |
| 사격 차단 | 캡처 단계에서 mousedown/mouseup 을 삼킨다. **단 포인터 락이 걸린 상태에서만** — 락을 얻기 위한 최초 클릭은 통과시켜야 한다 |
| 한글 HUD | DOM으로 직접 그린다 (목표 / 방송까지 남은 시간 / 카드 수집 현황) |
| 수집품 | 월드에 사연 카드 5개, XZ 평면 거리 2m 이내 자동 획득 |
| 대화 박스 | 화자 배지 + 타이핑 연출, 진행 중 입력을 캡처 단계에서 동결 |
| 진행 | 인트로 → 수집 → 부스 복귀 → 엔딩 |

---

## 4. 설계상 조심할 점

1. **리스너 등록 순서가 중요하다.**
   `_entered` 기록기 → `_eatFire`(사격 차단) → `_block`(대화 중 동결) 순서로 등록된다.
   캡처 리스너는 등록 순서대로 실행되므로, `_eatFire` 는 `this.paused` 일 때 반드시 그냥 반환해야
   `_block` 이 클릭을 받아 대사를 넘길 수 있다. 순서를 바꾸면 대화가 클릭으로 안 넘어간다.

2. **인트로는 포인터 락 획득 후에 시작한다.**
   부팅 직후 대화를 열면 시작 게이트 뒤에 가려진다. `state === 'boot'` 에서
   `pointerLockElement || _entered` 를 기다렸다가 연다. (락이 막힌 브라우저 대비로 `_entered` 도 함께 본다.)

3. **대화 중에는 수집이 멈춘다.** `update()` 가 `if (this.paused) return;` 로 빠져나가기 때문이다.
   의도된 동작이다. 자동 테스트를 짤 때는 **텔레포트 전에 열린 대화를 먼저 닫아야 한다.**
   이걸 몰라서 검증이 2/5, 4/5 로 들쭉날쭉했다.

4. **카운트다운은 0이 되어도 실패시키지 않는다.** 생방송 시연 중 하드 실패를 피하기 위한 결정이다.
   대사만 한 번 나오고 게임은 계속된다.

---

## 5. 검증

```bash
node tools/check.mjs        # 모듈 파싱 검사
node tools/storycheck.mjs   # 부팅 → 인트로 → 카드 5장 → 엔딩까지 자동 플레이
```

마지막 실행 결과:

```
booted        : true
카드 회수     : 5 / 5
엔딩 도달     : true
적 수         : 0 | 무기 숨김: true | 원본HUD 숨김: true
콘솔 이슈     : 0
```

라이브 배포 검증도 통과했다 — 로드+부팅 8.5초, 콘솔 에러 0.

> **주의**: 이 폴더의 playwright 는 1.55.0 으로 **의도적으로 다운그레이드**되어 있다.
> `npm install` 이 끌어온 1.62.0 은 이 환경에서 브라우저 실행에 실패한다(`spawn UNKNOWN`).
> 재설치했다가 검증이 깨지면 `fps-blacksite/node_modules` 의 playwright·playwright-core 를 복사해 오면 된다.

---

## 6. 다음에 손댈 만한 것

- **카드 위치 조정** — 현재 5장 전부 야외에 있다. 실내(-7.2, -4.6) 한 장을 빼면 난이도가 크게 낮아진다.
- **분기 대사** — 지금은 선형 진행이다. 카드 획득 순서에 따라 다른 대사를 넣을 수 있다.
- **레벨 드레싱** — 배경은 여전히 중동풍 시가지다. 스튜디오 소품(카메라·조명·큐시트 보드)을
  `Level.js` 에 추가하면 훨씬 그럴듯해진다. 다만 `Level.js` 는 5,003줄이라 작업량이 크다.
- **한글 폰트 임베드** — 지금은 시스템 폰트(Pretendard/맑은 고딕)에 의존한다.
  폰트가 없는 PC에서는 대체 폰트로 나온다.
- **모바일 대응** — 포인터 락 기반이라 현재 데스크톱 전용이다.

---

## 7. 대사 원문 위치

전부 `src/story/Story.js` 상단 상수에 모여 있다.

```
INTRO   인트로 5줄
PICKUP  카드별 획득 대사 (id → 줄 배열)
OUTRO   엔딩 3줄
TIMEUP  시간 초과 시 2줄
```

수정할 때는 이 상수만 고치면 된다. 다른 코드를 건드릴 필요가 없다.
